# `openid-client-discovery-caching-with-undici`

This repository shows how to use undici's composable agent system to cache
the result of a OpenID discovery request made with the package `openid-client`.
